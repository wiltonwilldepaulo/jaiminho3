{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Xdebug Docker",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "hostname": "0.0.0.0",
            "program": "",
            "pathMappings": {
                "/var/www": "${workspaceFolder}"
            }
        }
    ]
}